<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yin.dao.ChatMapper">

    <resultMap id="BaseResultMap" type="com.example.yin.domain.ChatMessage">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="role" property="role"/>
        <result column="ai_response" property="aiResponse"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, content, role, ai_response, create_time
    </sql>

    <select id="selectChatHistoryByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM chat_message
        WHERE user_id = #{userId}
        ORDER BY create_time ASC
    </select>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM chat_message
        WHERE id = #{id}
    </select>

    <insert id="insertChatMessage" parameterType="com.example.yin.domain.ChatMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (
            user_id,
            content,
            role,
            ai_response,
            create_time
        ) VALUES (
            #{userId},
            #{content},
            #{role},
            #{aiResponse},
            #{createTime}
        )
    </insert>

    <update id="updateChatMessage" parameterType="com.example.yin.domain.ChatMessage">
        UPDATE chat_message
        SET
            content = #{content},
            role = #{role},
            ai_response = #{aiResponse},
            create_time = #{createTime}
        WHERE id = #{id}
    </update>

</mapper>
